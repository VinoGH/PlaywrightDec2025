let X = 5
if (X > 0) {
        console.log ("The number "+ X +" is positive")
    } else if (X < 0) {
        return console.log ("The number "+ X +" is negative")
    } else {
        return console.log ("The number "+ X +" is zero")
    }